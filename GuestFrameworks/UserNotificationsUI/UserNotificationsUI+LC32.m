#import <UserNotificationsUI/UserNotificationsUI.h>

/* UserNotificationsUI 10.3 contains only a public protocol and a category.
 * Keep an image in the guest SDK so binaries linked against the framework can
 * load it even though there are no public data or C-function exports. */
